# Feature Documentation

This folder contains documentation for implemented features and current state.

**Updated**: 2026-09-12
**Recent**: Legacy residue removed — `src/format.ts` + `src/panel.ts` (unmounted cookie-era iteration), their tests, and the legacy `cordis.yml` endpoint are gone; codebase is apiKey-only with 22 tests.

## Index

1. [Kenari Usage Tool & Host Route](./01-kenari-usage-tool.md) — `kenari_usage` model tool + same-origin `GET /dsh-kenari-usage` (host half)
2. [Usage Dock UI](./02-usage-dock-ui.md) — floating draggable/resizable React card in the session header slot (client half)

---

**Catatan**: Tambahkan dokumen baru untuk fitur baru dengan nomor urut berurutan (misal terakhir `02-...md`, berikutnya `03-...md`). Perbarui dokumen yang ada saat fitur direvisi, dan selalu catat tanggal update di bagian atas README ini.
